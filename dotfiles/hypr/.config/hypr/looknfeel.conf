# Change the default Omarchy look'n'feel

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    # No gaps between windows or borders
    gaps_in = 0
    gaps_out = 0
    border_size = 1

    # Use master layout instead of dwindle
    # layout = master
}

animations {
  enabled = false
}

# AQ_NO_ATOMIC=1 (uwsm/env) puts aquamarine on the legacy DRM path, which breaks
# the AMD hardware cursor plane — cursor flickers/vanishes over windows. Force a
# software cursor. Remove if/when AQ_NO_ATOMIC goes away.
cursor {
    no_hardware_cursors = true
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    # Use round window corners
    # rounding = 8
}

# https://wiki.hypr.land/Configuring/Dwindle-Layout/
dwindle {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}

windowrule = opacity 1.0 override 1.0 override 1.0 override, match:class .*
